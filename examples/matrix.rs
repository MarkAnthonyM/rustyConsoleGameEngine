extern crate rustyConsoleGameEngine;

use rustyConsoleGameEngine::OlcConsoleGameEngine;

struct MatrixStrut {

}

fn main() {
    let matrix = MatrixStrut {

    };

    let closure = box::new(move |data| {

    });

    let demo = OlcConsoleGameEngine::new(closure, matrix);
}
